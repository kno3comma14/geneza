{:full-name "{{application_namespace}}/{{application_name}}"
 :ns-name   "{{application_namespace}}.{{application_name}}"
 :sanitized "{{application_namespace}}/{{application_name}}"
 :name      "{{application_name}}"
 :modules   {:root         "modules"
             :repositories [{:url  "https://github.com/kit-clj/modules.git"
                             :tag  "master"
                             :name "kit-modules"}]}}
